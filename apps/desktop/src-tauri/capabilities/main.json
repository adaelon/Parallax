{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "S08 main window: domain commands only; no plugin, generic file, shell, HTTP, process, or credential permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
