{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "S07 main window: core IPC only; no plugin, file, shell, HTTP, process, or credential permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
