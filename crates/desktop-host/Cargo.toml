[package]
name = "desktop-host"
version = "0.1.0"
edition = "2024"

[lib]
name = "eam_desktop_host"

[dependencies]
eam_core = { package = "core", path = "../core" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
pedantic = "deny"
