[package]
name = "retrieval"
version = "0.1.0"
edition = "2024"

[lib]
name = "eam_retrieval"

[dependencies]
eam_core = { package = "core", path = "../core" }
eam_ingestion = { package = "ingestion", path = "../ingestion" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
pedantic = "deny"
