[package]
name = "retrieval"
version = "0.1.0"
edition = "2024"

[lib]
name = "eam_retrieval"

[dependencies]
eam_core = { package = "core", path = "../core" }
eam_ingestion = { package = "ingestion", path = "../ingestion" }
sha2 = "=0.11.0"

[dev-dependencies]
eam_markdown = { package = "eam-markdown", path = "../markdown" }

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "deny"
pedantic = "deny"
