[package]
name = "ingestion"
version = "0.1.0"
edition = "2024"

[dependencies]
eam_markdown = { package = "eam-markdown", path = "../markdown" }

[lib]
name = "eam_ingestion"

[dev-dependencies]
tempfile = "=3.27.0"

[lints.rust]
unsafe_code = "deny"

[lints.clippy]
all = "deny"
pedantic = "deny"
